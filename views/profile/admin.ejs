
<body>
    <h2 class="banner">Super secret! -- Only Admins Allowed!</h2>
        <div>
            <strong>Name:</strong><%= user.firstname %>'s Admin Page
            <div>
                <img src="<%= user.profile %>" alt="user profile pic">
            </div>
            <div>
                <strong>Name:</strong><%= user.firstname %> <%= user.lastname %> 
                <p>
                    <strong>Admin:</strong><%= user.admin ? 'Yes' : 'No' %>
                </p>
            </div>
            <div>
                <%= user.bio %>
            </div>
        </div>
</body>
